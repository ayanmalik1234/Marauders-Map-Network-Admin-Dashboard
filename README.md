# Marauder's Map Network Admin Dashboard

Open [index.html](index.html) directly in a browser. No Node.js or package install is required.

The page includes a live castle blueprint, searchable endpoint directory, telemetry profile view, incident response controls, and simulated movement logs.
